[project]
name = "dbl-vlog"
version = "0.3.0"
description = "DBL event substrate V with deterministic canonicalization and digests"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
